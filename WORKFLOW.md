# Workflow · 古籍转译工作流

> 把"我想转译一本古籍"这个念头,**变成一条可被任何人复用的工程流**。

---

## 总流程:八步飞轮

```text
选书 → 建档 → 底本 → 转录 → 断句 → 校注 → 白话 → 校验 → 发布
   ↓      ↓      ↓      ↓      ↓      ↓      ↓      ↓      ↓
  R-0   F-0    E-0   T-1    P-1    A-1   V-1    V-2    P-2
```

每一步都有产物、有审校、有退出条件。不是写完就算完。

---

## Step 0 · 选题(R-0 立项)

**问题**:要转译哪本书?为什么是这本书?

**必填**:

- `work_id`(项目唯一标识,小写,如 `ddj-c01` / `zdys-c03`)
- 选题理由(为什么这部书值得转译?填补了什么空白?)
- 优先级判断(经典 vs 小众 / 完整 vs 节选)

**退出条件**:

- 一段不超过 5 行的"立项说明",解释这部书对今天的读者意味着什么
- 否则不进入下一步

**反证陷阱**:

- ❌ 只因为"我想做"立项 — 没有清晰选题理由的项目,做完也没人用
- ❌ 选了一本没人校勘过的孤本 — 无参校本,无法做异文对比

---

## Step 1 · 建档(F-0 Frontmatter)

**问题**:这本古籍的元信息是什么?

**产物**:`.rt` 文件的 YAML frontmatter(协议规定字段)。

**必填字段**:

| 字段              | 示例                | 说明           |
|-----------------|-------------------|--------------|
| `title`         | 道德经·第一章          | 书名+卷次        |
| `work_id`       | ddj-c01           | 唯一标识         |
| `author`        | 老子                 | 作者           |
| `dynasty`       | 春秋                 | 朝代/成书年代      |
| `editions.base` | 王弼本(魏晋)          | 主底本          |
| `editions.collated` | 列表              | 参校本列表        |
| `protocol`      | rtp/0.1           | 协议版本         |
| `created`       | 2026-09-20         | 创建日期(ISO)    |
| `transliterator` | 文川院 AI v1       | 转译引擎         |

**退出条件**:

- 所有必需字段填写完整
- `work_id` 在 `08 索引/已立项项目.md` 中唯一

---

## Step 2 · 底本确认(E-0 Editions)

**问题**:用哪个版本作为底本?哪些版本做参校?

**判断依据**:

- **传世本 vs 出土本**:有出土本(如帛书、简牍)优先作为参校
- **影响最大版本**:学术影响力最大者作底本
- **可获取性**:底本必须可全文获取(不是孤本/未公开)

**反证陷阱**:

- ❌ 用"通行本"作底本却不说 — 通行本未必是最优底本
- ❌ 参校本过多(>5 个)却不做优先级 — 校验器无法工作

---

## Step 3 · 转录(T-1 Source)

**问题**:原文怎么录入?

**规则**:

1. **逐字保留** — 一字不改,包括异体字、避讳字、讹字
2. **保留行款** — 原文换行 / 段位尽量保留
3. **编码规范** — 异体字用 Unicode,不要造字
4. **存疑标注** — 看不清的字用 `[?]` 占位,并在 `meta` 区说明

**操作**:参考 `prompts/transcribe.md`

**退出条件**:

- 原文逐字录入完成,无 AI 自动"修正"
- 避讳字 / 异体字在 `annotation` 区说明

---

## Step 4 · 断句(P-1 Punctuation)

**问题**:现代标点怎么加?依据是什么?

**原则**:

- 优先采用学界共识断句(王力、郭锡良等)
- 同一文本有多种断句时,**并列保留**,用 `<!-- @alt: punctuation -->` 区段

**操作**:参考 `prompts/punctuate.md`

**反证陷阱**:

- ❌ AI 自行断句不说明依据 — 协议强制要求 `@校断` 注明出处
- ❌ 古典散文当骈文断句 — 标点密度应反映文体

---

## Step 5 · 校注(A-1 Annotation)

**问题**:哪些字词需要注释?注释什么内容?

**校注五类**:

| 类型    | 标记       | 适用                 |
|-------|----------|--------------------|
| 校勘    | `@校勘`    | 异文、讹误、脱衍              |
| 音注    | `@音注`    | 生僻字读音、通假字             |
| 训诂    | `@训诂`    | 字义、引申义                |
| 字源    | `@字源`    | 汉字形体演变、出处              |
| 出注    | `@出注`    | 引文出处、其他书互证             |

**操作**:参考 `prompts/annotate.md`

**退出条件**:

- 关键术语、生僻字、典故全覆盖
- 每条校注引用来源(王弼《老子注》、孔颖达《正义》等)

---

## Step 6 · 白话(V-1 Vernacular)

**问题**:现代汉语白话译怎么写?

**原则**:

1. **信** — 不增不减,逐句对应
2. **达** — 现代汉语口语,不用文言词
3. **雅** — 保留原文韵味,避免翻译腔

**操作**:参考 `prompts/vernacular.md`

**反证陷阱**:

- ❌ 译文中夹注解释 — 解释放 `annotation`,不放白话区
- ❌ 用网络流行语翻译经典 — 失去经文的庄重感

---

## Step 7 · 校验(V-2 Validate)

**问题**:这份 `.rt` 是否符合协议?

**校验器**:`tools/validator.py`(命令行)

```bash
python tools/validator.py examples/道德经-第一章.rt
```

**必查项**:

1. frontmatter 必需字段齐全
2. 协议版本号有效
3. 区段顺序正确
4. 异文表列名与参校本一致
5. 校注引用与原文字符对应

**退出条件**:校验器返回 0 退出码

---

## Step 8 · 发布(P-2 Publish)

**问题**:如何让读者读到?

**发布渠道**:

- **GitHub Pages** — 项目站
- **静态网站生成** — 把 `.rt` 批量渲染为 HTML
- **PDF / EPUB** — 沿用 Rivertype 渲染引擎 + `themes/guji` 古籍主题
- **API** — 协议化的结构化数据,允许第三方应用消费

**资产提取**(`06 资产账本`):

每次转译完成后,从 `.rt` 文件中提取:

- 概念资产(被命名的术语解释)
- 案例资产(经过分析的句子)
- 叙事资产(可复用的故事)
- 决策资产(校勘取舍的理由)

---

## 协作时的角色分工

| 角色      | 职责                | 谁可以做       |
|---------|-------------------|------------|
| 立题者    | Step 0-1,选题与建档    | 项目负责人       |
| 校勘者    | Step 2-3,定底本、转录    | 古典文献专业      |
| 断句者    | Step 4,标点与依据      | 古典文献专业      |
| 注疏者    | Step 5,校注          | 古典文献专业/历史  |
| 译述者    | Step 6,白话译        | 现代汉语母语      |
| 校验者    | Step 7,运行校验器      | AI/工程师      |
| 发布者    | Step 8,渲染与发行      | 编辑/工程师      |

**核心纪律**:

- **每个角色只对自己的产物负责** — 译述者不擅改原文,校勘者不擅译白话
- **每个产物都有版本号** — 任何修改都要走 git commit
- **每个判断都要有反证区** — 校勘取舍要写下理由

---

## 反证区:什么时候这套工作流会失败?

1. **孤本无参校** — 没有第二个版本,异文表是空的,转译失去校勘意义
2. **AI 幻觉字句** — AI 在 `source` 区添字 — 校验器需加"原文与原始资料逐字比对"
3. **底本争议** — 学界对底本尚无共识时,必须显式标注"底本未决"
4. **白话过度意译** — 译述者把"道"译成"宇宙规律"而非直译 — 需要术语表约束
5. **校注膨胀** — 校注喧宾夺主,注释长度超过原文 — 应另出《校注集》

每发生一次失败案例,必须写进 `docs/protocol/COUNTER-EVIDENCE.md`。

---

## 进入下一步

每一次完成 Step 8,必须执行**研究收口动作**(见文川院规则):

1. 产出 `.rt` 文件 ✓
2. 提取 3-10 个可复用资产 ✓
3. 写入 `asset_ledger.json` ✓
4. 标注转化管线(article / card / course_module / ...) ✓
5. 更新 `current_state.json` 的 `next_action` ✓
