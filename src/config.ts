export const API_BASE = '/api';

export interface AppConfig {
  brand: string;
  slogan: string;
  defaultPlaceholder: string;
}

export const config: AppConfig = {
  brand: 'RIVERTYPE',
  slogan: '未来出版引擎',
  defaultPlaceholder: '在此输入内容...',
};
