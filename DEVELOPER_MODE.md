# Rivertype 开发者模式

## 架构理念

去中心化思想同样适用于开发环境：每个工具专注做它最擅长的事。

```
┌─────────────────────────────────────────────────────────────┐
│                        Cursor (中控)                          │
│           代码编写 · 需求分析 · 文档管理 · 协调调度             │
└────────────────────────┬─────────────────┬───────────────────┘
                         │                 │
              ┌──────────▼──────┐  ┌───────▼─────────────┐
              │     PyCharm     │  │       Chrome        │
              │    后端开发     │  │      前端预览        │
              │                 │  │                      │
              │  • 断点调试     │  │  • Live Reload      │
              │  • 变量监控     │  │  • DevTools         │
              │  • 单元测试     │  │  • Network 调试     │
              │  • 终端输出     │  │  • Console          │
              └─────────────────┘  └──────────────────────┘
```

## 工具职责

| 工具 | 职责 | 优势 |
|------|------|------|
| **Cursor** | 全局协调、代码编写、文档 | AI 辅助、跨文件编辑、智能提示 |
| **PyCharm** | Python 后端、调试、依赖管理 | 强大的 Python 调试能力、断点、REPL |
| **Chrome** | 前端预览、浏览器调试 | 实时刷新、DevTools、Network 分析 |

## 启动步骤

### 1. 启动后端 (PyCharm)

```bash
# 在 PyCharm 中打开 backend/ 文件夹
# 运行 main.py
# 默认端口: http://127.0.0.1:8000
```

### 2. 启动前端 (Chrome)

```
http://127.0.0.1:8000
```

### 3. 开始开发 (Cursor)

在 Cursor 中进行所有代码编写和修改工作。

## 工作流程

```
1. Cursor 分析需求，编写/修改代码
2. PyCharm 运行后端，自动加载代码变更
3. Chrome 刷新页面，查看效果
4. 如需调试后端，在 PyCharm 中打断点
5. 如需调试前端，在 Chrome DevTools 中操作
```

## 注意事项

- 修改 Python 代码后，PyCharm 会自动重新加载（debug 模式）
- 前端页面可随时刷新，无需重启后端
- 复杂调试任务交给 PyCharm，简单预览交给 Chrome
- Cursor 始终作为主协调者，避免在多个 IDE 间来回切换

## 端口说明

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 8000 | FastAPI 服务 |
| Ollama AI | 11434 | 本地 AI 模型（可选） |

## 未来扩展

当需要同时开发前后端复杂功能时，可以考虑：

- **前后端分离**：前端用 `python -m http.server 3000` 独立服务
- **WebSocket 调试**：PyCharm 调试 WebSocket 连接
- **Docker 环境**：统一开发环境配置
