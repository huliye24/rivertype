# RiverType · 古籍现代化转译协议与工具
# 核心依赖：CLI 流水线与 RTP 校验器

# CLI 内核（PyYAML 用于 book.yaml 与 depth 表解析）
PyYAML>=6.0

# Property-based testing（depth ADT 测试）
hypothesis>=6.0

# 测试框架
pytest>=8.0

# PDF 渲染（assemble / build）
WeasyPrint>=60.0

# EPUB 输出
ebooklib>=0.18

# CLI 与 PDF 渲染可能用到的可选依赖（按需安装）：
#   pip install rivertype-cli[anthropic]   # Claude 视觉转录
#   pip install rivertype-cli[openai]      # GPT-4o 视觉转录
#   pip install rivertype-cli[ollama]      # 本地多模态模型
